# =====================================================================
# Frontend (apps/web) — copie pra `.env.local` e ajuste.
# =====================================================================
VITE_API_URL=http://localhost:3001

# Clerk — dashboard.clerk.com → seu app → API keys.
# Começa com `pk_test_` em dev e `pk_live_` em produção.
VITE_CLERK_PUBLISHABLE_KEY=
